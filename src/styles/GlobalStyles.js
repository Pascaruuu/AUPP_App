import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({

  container: {
    flexGrow: 1,
    padding: 20,
    backgroundColor: '#fff', 
  },

  title: {
    fontSize: 11,
    fontWeight: 'bold',
    color: '#333',
  },

  description: {
    fontSize: 10,
    color: '#666',
  },
});